<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.listagem">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Pagina3"></activity>
        <activity android:name=".SQLhelper" />
        <activity android:name=".Lista" />
        <activity android:name=".Livros" />
        <activity
            android:name=".Pagina2"
            android:label="@string/title_activity_pagina2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Pagina1"
            android:label="@string/title_activity_pagina1"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PaginaInicial"
            android:label="@string/title_activity_pagina_inicial"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Tela1"
            android:label="@string/app_name" />
    </application>

</manifest>